#include <stdio.h>
#include "main.h"
/**
 * main - prints the name of the program
 * @argc: args count
 * @argv: array vector
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
